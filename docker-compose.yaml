services:
  main:
    build:
      context: .
      dockerfile: Dockerfile
    restart: "unless-stopped"
    ports:
      - "7001:7001"
    env_file:
      - .env
    volumes:
      - logs:/var/log/bot-seller:/app/logs
volumes:
  logs: